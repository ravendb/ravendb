<Infrastructure:View x:Class="Raven.Studio.Home"
					 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					 xmlns:Infrastructure="clr-namespace:Raven.Studio.Infrastructure"
					 xmlns:Documents="clr-namespace:Raven.Studio.Features.Documents"
					 Title="Home"
					 Style="{StaticResource PageStyle}"
					 Infrastructure:ModelAttacher.AttachObservableModel="Raven.Studio.Models.HomeModel">

	<Grid DataContext="{Binding Value}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		
		
		<HyperlinkButton Content="New Database"
						 Command="{Binding Path=CreateNewDatabase}" />
		
		<Documents:DocumentsView DataContext="{Binding Path=RecentDocuments.Value}"
								 Grid.Row="1"
								 Margin="{StaticResource Margin_ContentPage}"
								 Tag="Recent Documents" />
	</Grid>
</Infrastructure:View>
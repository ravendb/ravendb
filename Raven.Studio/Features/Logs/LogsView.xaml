<Infrastructure:View x:Class="Raven.Studio.Features.Logs.LogsView"
					 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					 xmlns:Infrastructure="clr-namespace:Raven.Studio.Infrastructure"
					 xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
					 xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="2*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<data:DataGrid x:Name="LogsList" AutoGenerateColumns="True"
					   IsReadOnly="True"
					   ItemsSource="{Binding Logs}" />

		<sdk:GridSplitter Grid.Row="1"
						  Background="Transparent"
						  HorizontalAlignment="Stretch" />
		
		<Grid Grid.Row="2">
			<TextBlock Text="{Binding ElementName=LogsList, Path=SelectedItem}" />
		</Grid>
	</Grid>
</Infrastructure:View>
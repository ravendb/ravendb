<UserControl x:Class="Raven.Studio.Features.Settings.DatabaseSettingsSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Editors="clr-namespace:Raven.Studio.Controls.Editors"
             xmlns:Models="clr-namespace:Raven.Studio.Models" mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" d:DataContext="{d:DesignInstance Models:DatabaseSettingsSectionViewModel}">
    
    <Grid x:Name="LayoutRoot">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <HyperlinkButton Content="Edit"
									Command="{StaticResource NavigateToCommand}"
									CommandParameter="{Binding CurrentDatabase, StringFormat='/edit?databaseid=\{0\}'}"/>
            <Editors:JsonEditor BoundDocument="{Binding DatabaseEditor}" Grid.Row="1"/>
    </Grid>
</UserControl>

// we want same build scripts for 4.2 and up
// so in all versions we define 2 npm scripts:
// npm run restore - in 5.3+ it does nothing
// npm run compile - runs webpack compilation

console.log("Nothing to restore");

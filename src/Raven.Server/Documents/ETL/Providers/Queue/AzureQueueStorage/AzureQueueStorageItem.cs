namespace Raven.Server.Documents.ETL.Providers.Queue.AzureQueueStorage;

public sealed class AzureQueueStorageItem : QueueItem
{
    public AzureQueueStorageItem(QueueItem item) : base(item)
    {
    }
}

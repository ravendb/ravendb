import { Alert } from "@/components/shadcn/ui/alert";

export function BindChannelsStep() {
    return (
        <Alert>
            Channel binding isn&apos;t available yet. Save the agent now — you&apos;ll be able to expose it through web
            widgets and bots once channel management ships.
        </Alert>
    );
}
